torch>=2.0.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
streamlit>=1.32.0
joblib>=1.3.0

# ── How to install ─────────────────────────────────────────────
# Standard (all platforms):
#   pip install -r requirements.txt
#
# Windows (if torch is slow):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
#
# Google Colab (torch/numpy/pandas already installed):
#   !pip install streamlit joblib
